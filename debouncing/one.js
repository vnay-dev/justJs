let counter = 0;

function getData() {
  console.log("Data fetch api called...", counter++);
}

// every time we type something inside the input box
// the function gets called
// this needs to be changed to 
// the function needs to be called every time we make a pause while typing
